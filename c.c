#include<stdio.h>
#include<conio.h>
int main(){
    int a,b,c,d=0;
    printf("Enter the values for four numbers");
    scanf("%d %d %d %d",&a,&b,&c,&d);
    /*if(a>b&&a>c&&a>d)printf("a is greatest");
    if(b>a&&b>c&&b>d)printf("b is greatest");
    if(c>a&&c>b&&c>d)printf("c is greatest");
    if(d>a&&d>b&&d>c)printf("d is greatest");*/
    return 0;
}